---
import { storyblokEditable } from "@storyblok/astro";

const { blok } = Astro.props;
---

<style>
    .menu{
        height: 5.6rem;
        border-bottom: 1px solid #DDDDDD;
        border-top: 1px solid #DDDDDD;
        display: flex;
        align-items: center;
        justify-content: center;
    }
</style>

<div class="menu" {...storyblokEditable(blok)}>
    <p>MENU</p>
</div>